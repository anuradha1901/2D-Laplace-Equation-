# FINALPROJECT: is the target name.
# COMP526.c: is the dependency/prerequisite. This is the source file.
# It is followed by the command, used for compilation, the object name here is Final
# gcc -o (object name) (program name) (dependency)

FINALPROJECT: COMPF526.c
	gcc -o Final COMPF526.c -lm

# Run using ./Final command
# This is used to remove the created object whenever you type
# make clear
# This is optional, but very useful
clear:
	rm FINALPROJECT
